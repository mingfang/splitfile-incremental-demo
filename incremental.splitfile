FROM ${DESTINATION}

SQL {
    CREATE TABLE IF NOT EXISTS ${TABLE}
    (LIKE "${SOURCE}".${TABLE}
    INCLUDING ALL)
}

${EXTRA_SQL}

SQL{
    INSERT INTO ${TABLE} (
        SELECT * FROM "${SOURCE}".${TABLE}
    )
}