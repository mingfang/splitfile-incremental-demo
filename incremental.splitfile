FROM ${DESTINATION}

SQL {
    CREATE TABLE IF NOT EXISTS ${TABLE}
    AS TABLE "${SOURCE}".${TABLE}
    WITH NO DATA
}
SQL{
    INSERT INTO ${TABLE} (
        SELECT * FROM "${SOURCE}".${TABLE}
    )
}